{
  "name": "hide-github-diff",
  "version": "1.0.0",
  "description": "",
  "main": "content.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 src/content.js > dist/content.js && babel --presets es2015 src/popup.js > dist/popup.js && babel --presets es2015 src/event.js > dist/event.js && cp src/popup.html dist/popup.html && cp src/manifest.json dist/manifest.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "6.24.0",
    "babel-preset-es2015": "6.24.0"
  }
}
